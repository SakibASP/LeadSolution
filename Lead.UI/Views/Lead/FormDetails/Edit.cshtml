@model Core.Models.Lead.FormDetails

@{
    ViewData["Title"] = "Edit Form Detail";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow rounded-3">
                <div class="card-header bg-primary text-white text-center">
                    <h4 class="mb-0">Edit Form Detail</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        @* Hidden Fields *@
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="CreatedBy" />
                        <input type="hidden" asp-for="CreatedDate" />

                        <div class="form-group mb-3">
                            <label asp-for="Name" class="form-label fw-bold"></label>
                            <input asp-for="Name" class="form-control" placeholder="Enter field name" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="TypeId" class="form-label fw-bold"></label>
                            <select asp-for="TypeId" class="form-select" asp-items="ViewBag.TypeId">
                                <option disabled selected>-- Select Type --</option>
                            </select>
                            <span asp-validation-for="TypeId" class="text-danger"></span>
                        </div>

                        <div class="form-check form-switch mb-2">
                            <input class="form-check-input" asp-for="IsNullSupported" type="checkbox" />
                            <label class="form-check-label fw-semibold" asp-for="IsNullSupported"></label>
                        </div>

                        <div class="form-check form-switch mb-2">
                            <input class="form-check-input" asp-for="IsSelectInput" type="checkbox" />
                            <label class="form-check-label fw-semibold" asp-for="IsSelectInput"></label>
                        </div>

                        <div class="form-check form-switch mb-4">
                            <input class="form-check-input" asp-for="IsActive" type="checkbox" />
                            <label class="form-check-label fw-semibold" asp-for="IsActive"></label>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-save"></i> Save Changes
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="text-center mt-3">
                <a asp-action="Index" class="text-decoration-none">
                    ← Back to List
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
